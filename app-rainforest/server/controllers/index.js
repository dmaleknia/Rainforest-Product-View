const answerqs = require('./answerqs');
const descriptions = require('./descriptions');
const prices = require('./prices');
const products = require('./products');
const productSizes = require('./productsizes');
const retailers = require('./retailers');
const reviews = require('./reviews');
const users = require('./users');

//all controllers will be exported from this file.
module.exports = {
  answerqs,
  descriptions,
  prices,
  products,
  productSizes,
  retailers,
  reviews,
  users,
}